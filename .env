# Database (Docker PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASSWORD=password
DB_NAME=urlshortener

# Redis (if using Docker Redis too)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# App
APP_PORT=8080
BASE_URL=http://localhost:8080